import UserSettings from "./userSettings.controller";

const moduleName = "chatApp.userSettings";

angular.module(moduleName, []).controller("UserSettings", UserSettings);

export default moduleName;
